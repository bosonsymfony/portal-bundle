{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TileGroup list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Active</th>
                <th>Size</th>
                <th>Title</th>
                <th>Titleforeground</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tilegroup_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.active }}</td>
                <td>{{ entity.size }}</td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.titleForeground }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tilegroup_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tilegroup_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tilegroup_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
