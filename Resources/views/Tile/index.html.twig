{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tile list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Selected</th>
                <th>Backgroung</th>
                <th>Foreground</th>
                <th>Size</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tile_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.selected }}</td>
                <td>{{ entity.backgroung }}</td>
                <td>{{ entity.foreground }}</td>
                <td>{{ entity.size }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tile_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tile_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tile_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
